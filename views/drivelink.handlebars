<!-- filepath: views/alterar-link-drive.handlebars -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{> _msg}}
    <title>Alterar Link do Drive</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .navbar {
            background: linear-gradient(90deg, rgba(255, 223, 0, 1) 0%, rgba(255, 200, 0, 1) 50%, rgba(255, 175, 0, 1) 100%);
            padding: 1rem;
            border-radius: 8px;
        }
        .navbar .nav-list {
            display: flex;
            justify-content: center;
            align-items: center;
            list-style: none;
            padding: 0;
            margin: 0;
            width: 100%;
        }
        .navbar .nav-item {
            margin: 0 1rem;
        }
        .navbar .nav-link {
            color: white;
            text-decoration: none;
            transition: color 0.3s ease, transform 0.3s ease;
        }
        .navbar .nav-link:hover {
            color: #000000;
            transform: scale(1.1);
        }
        .navbar .nav-link.active {
            font-weight: bold;
            color: #ffcc00;
        }
        .header-title {
            font-family: 'Arial', sans-serif;
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 1rem;
            color: black;
        }
        .main-content {
            flex: 1;
        }
        .btn-hover {
            transition: transform 0.2s ease-in-out;
        }
        .btn-hover:hover {
            transform: scale(1.05);
        }
        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .card-body {
            padding: 20px;
        }
        .form-control {
            margin-bottom: 15px;
        }
        .footer {
            background-color: #343a40;
            color: white;
            padding: 1rem 0;
            text-align: center;
        }
    </style>
</head>
<body>
    <header class="header py-3">
        <div class="container">
            <h1 class="header-title">Alterar Link do Drive</h1>
            <nav class="navbar">
                <ul class="nav-list">
                    <li class="nav-item"><a class="nav-link" href="/cliente">Voltar</a></li>
                </ul>
            </nav>
        </div>
    </header>
    <main class="container my-5 main-content">
        <h2>Lista de Clientes</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Link do Drive</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                {{#each clients}}
                <tr>
                    <td>{{this.nome}}</td>
                    <td>{{this.drive_link}}</td>
                    <td>
                        <form action="/admin/edit/drive/update/{{this.client_id}}" method="POST">
                            <div class="form-group">
                                <input type="text" name="drive_link" class="form-control" value="{{this.drive_link}}" required>
                            </div>
                            <button type="submit" class="btn btn-primary btn-hover">Salvar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </main>
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 FIKC. Todos os direitos reservados.</p>
        </div>
    </footer>
</body>
</html>