{{> _msg}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/assets/favicon.jpg" />
    <title>Relatórios</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: "Montserrat", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
            background: #fff;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: #f9f9f9;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
            font-size: 2em;
            margin-bottom: 20px;
        }
        .filter-form {
            margin-bottom: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .filter-form input, .filter-form button {
            padding: 10px;
            font-size: 1em;
        }
        ul {
            list-style: none;
            padding: 0;
        }
        li {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: #fff;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        li:hover {
            transform: translateY(-5px);
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
        }
        li strong {
            font-size: 1.2em;
            color: #333;
        }
        li a {
            color: #ffcc00;
            text-decoration: none;
            font-weight: bold;
        }
        li a:hover {
            text-decoration: underline;
        }
        li small {
            display: block;
            margin-top: 5px;
            color: #666;
        }
        .no-reports {
            text-align: center;
            font-size: 1.2em;
            color: #999;
            margin-top: 20px;
        }
        /* Estilo para o botão de filtrar */
        .filter-form button {
            padding: 10px 20px;
            font-size: 1em;
            font-weight: bold;
            color: #fff; /* Texto branco */
            background-color: #ffcc00; /* Amarelo principal */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .filter-form button:hover {
            background-color: #e6b800; /* Amarelo mais escuro */
            transform: scale(1.05); /* Leve aumento no tamanho */
        }

        .filter-form button:active {
            background-color: #cc9900; /* Amarelo ainda mais escuro */
            transform: scale(0.95); /* Leve redução no tamanho */
        }

        .back-button {
            display: inline-block;
            margin-bottom: 20px;
            padding: 10px 20px;
            font-size: 1em;
            font-weight: bold;
            color: #fff;
            background-color: #ffcc00; /* Amarelo principal */
            border: none;
            border-radius: 5px;
            text-decoration: none;
            text-align: center;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .back-button:hover {
            background-color: #e6b800; /* Amarelo mais escuro */
            transform: scale(1.05); /* Leve aumento no tamanho */
        }

        .back-button:active {
            background-color: #cc9900; /* Amarelo ainda mais escuro */
            transform: scale(0.95); /* Leve redução no tamanho */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Seus Relatórios</h1>
        <a href="/cliente-unico/{{client_id}}" class="back-button">Voltar para a Área do Cliente</a>
        <form class="filter-form" method="GET" action="/cliente-unico/{{client_id}}/relatorios">
            <input type="text" id="nome" name="nome" placeholder="Filtrar por nome">
            <input type="date" id="data_inicio" name="data_inicio" placeholder="Data início">
            <input type="date" id="data_fim" name="data_fim" placeholder="Data fim">
            <button type="submit">Filtrar</button>
        </form>
        <div id="relatorios-container">
            {{#if arquivos.length}}
                <ul>
                    {{#each arquivos}}
                        <li>
                            <strong>{{nome_original}}</strong><br>
                            <a href="/{{caminho}}" target="_blank">Visualizar</a><br>
                            <small>Enviado em: {{formatDate data_upload}}</small>
                        </li>
                    {{/each}}
                </ul>
            {{else}}
                <p class="no-reports">Nenhum relatório enviado ainda.</p>
            {{/if}}
        </div>
    </div>
</body>
</html>
